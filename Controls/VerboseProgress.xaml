<mah:MetroContentControl x:Class="DownTube.Controls.VerboseProgress"
                         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                         xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                         xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                         xmlns:local="clr-namespace:DownTube.Controls"
                         mc:Ignorable="d"
                         d:DesignHeight="30" d:DesignWidth="450">
    <mah:MetroContentControl.DataContext>
        <local:VerboseProgress_ViewModel />
    </mah:MetroContentControl.DataContext>
    <Grid>
        <mah:MetroProgressBar Value="{Binding Progress}" IsIndeterminate="{Binding Indeterminate}" Minimum="0"
                              Maximum="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
        <Label Content="{Binding ProgressText}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
               HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Opacity="0.5" ForceCursor="False" />
    </Grid>
</mah:MetroContentControl>