<UserControl x:Class="DownTube.Views.Controls.FolderPicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:WPFUI.Controls;assembly=WPFUI"
             mc:Ignorable="d"
             d:DesignHeight="40"
             d:DesignWidth="500"
             d:DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <DockPanel>
        <Button DockPanel.Dock="Right"
                VerticalAlignment="Stretch"
                Width="120"
                Padding="0"
                Margin="5,0,0,0">
            <Button.Content>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Browse"
                               Margin="0,0,10,0" />
                    <controls:Icon Glyph="Folder48" />
                </StackPanel>
            </Button.Content>
        </Button>
        <TextBox x:Name="TB"
                 LostKeyboardFocus="TextBox_LostKeyboardFocus"
                 PreviewKeyDown="TextBox_PreviewKeyDown" VerticalContentAlignment="Center" />
    </DockPanel>
</UserControl>
